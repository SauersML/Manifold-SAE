# Paste into one Colab cell. Runtime → Change runtime type → T4 GPU first.
# Self-contained: uses absolute paths so it's safe to re-run after the
# script's working directory has been deleted.

%cd /content
!nvidia-smi | head -5
!rm -rf /content/Manifold-SAE
!git clone -q https://github.com/SauersML/Manifold-SAE.git /content/Manifold-SAE
!pip install -q "gamfit>=0.1.81" transformers datasets scipy
!cd /content/Manifold-SAE && python -m experiments.llm_real
